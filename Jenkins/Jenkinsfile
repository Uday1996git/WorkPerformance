properties([

    parameters([
        booleanParam(name: 'Refresh', defaultValue: false, description: 'Read Jenkinsfile and exit.'),
        separator(name: "Service", sectionHeader: "Choose a service to run Load on"),
    ])
])

pipeline{

    agent any

}