@Library('cloud-services-pipeline-shared-library') _
properties([
    parameters([
        booleanParam(name: 'Refresh', defaultValue: false, description: 'Read Jenkinsfile and exit.')
    ])
])

pipeline{

    agent any

}