{
  "name": "raster-server",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "raster-server/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "tscbuild": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "load-tests/raster-server/dist",
        "main": "load-tests/raster-server/src/index.ts",
        "tsConfig": "load-tests/raster-server/tsconfig.lib.json",
        "assets": ["load-tests/raster-server/*.md"]
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "load-tests/raster-server/**/*.ts",
          "load-tests/raster-server/package.json"
        ]
      }
    },
    "build": {
      "executor": "nx:run-script",
      "options": {
        "script": "build-webpack"
      }
    },
    "test": {
      "executor": "nx:run-script",
      "options": {
        "script": "run-tests"
      }
    },
    "testd": {
      "executor": "nx:run-script",
      "options": {
        "script": "run-testsd"
      }
    }
  }
}
